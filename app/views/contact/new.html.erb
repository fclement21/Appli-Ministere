<!-- Ce que l'utilisateur rempli et envoi -->
<div class="section-index">
<%= form_for @issue, :url => contact_path do |form| %>
  <fieldset class="fields">
    <div class="field">
      <%= form.label :subject %>
      <%= form.text_field :subject %>
    </div>
<div class="field">
<%=form.label :project_id %>
<%= form.text_field :project_id %>
</div>
    <div class="field">
      <%= form.label :priority_id %>
      <%= form.text_field :priority_id %>
    </div>


    <div class="field">
      <%= form.label :description %>
      <%= form.text_area :description, :placeholder => 'Ecrire le corps de votre message ainsi que un ou des emails supplémentaires' %>
    </div>
  </fieldset>

  <fieldset class="actions">
    <%= form.submit "Send" %>
  </fieldset>
<% end %>
</div>
